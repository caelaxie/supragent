interface:
  display_name: "Model Detector"
  short_description: "Find exact model IDs from runtime evidence"
  default_prompt: "Use $model-detector to identify the exact model used by this run from first-party evidence."
